# Minimal dependencies for Render deployment (Backend API only)
# Heavy ML dependencies (torch, transformers, etc.) are excluded
# Those are only needed for data collection scripts, not the API

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
pymongo==4.6.0

# AI API Client
openai>=1.12.0

# Utilities
python-dotenv==1.0.0
pydantic==2.5.0
python-dateutil==2.8.2
numpy==1.26.2

# Optional (if used in your code)
requests==2.31.0
